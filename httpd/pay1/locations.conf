    <Location /amialive.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      PerlOptions +ParseHeaders
      Options +ExecCGI
    </Location>

    <Location /payment/xml2.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      PerlOptions +ParseHeaders
      Options +ExecCGI
    </Location>

    <Location /payment/xml.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      PerlOptions +ParseHeaders
      Options +ExecCGI
    </Location>

    <Location /test2.cgi>
      PerlResponseHandler ModPerl::Registry
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlOptions +ParseHeaders 
      Options +ExecCGI
    </Location>

    <Location /admin/smps2.cgi>
      PerlResponseHandler ModPerl::Registry
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlOptions +ParseHeaders +GlobalRequest
      Options +ExecCGI
    </Location>

    <Location /admin/virtualtermdev.cgi>
      PerlResponseHandler ModPerl::Registry
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlOptions +ParseHeaders +GlobalRequest
      Options +ExecCGI
    </Location>

    <LocationMatch "/admin/(smps|virtualterm).cgi">
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      PerlOptions +ParseHeaders
      Options +ExecCGI
    </LocationMatch>

    <Location /cgi-bin/forbidden.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/remotepos.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/pnpremote.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /admin/signature_image.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>
 
    <Location /payment/collectbilling.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>
 
    <Location /payment/processbilling.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <LocationMatch "/payment/(pay|auth).cgi">
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </LocationMatch>

    #<LocationMatch "/payment/(golinte1|homeclip|dietsmar|nutricisec|metropolen|cheapcarib|prankplace|mdt|harddriv|titanmed)pay.cgi">
    <LocationMatch "/payment/(nutricisec|mdt|test)pay.cgi">
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </LocationMatch>

    <LocationMatch "/payment/(mexicosprin|cyclebuy|lindaone)pay.cgi">
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </LocationMatch>

    #<LocationMatch "/payment/(coolonlinep|coolonlinep1|coolonlinep2)pay.cgi">
    #  PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
    #  PerlOptions +ParseHeaders
    #  SetHandler perl-script
    #  PerlResponseHandler ModPerl::Registry
    #  Options +ExecCGI
    #</LocationMatch>

    <LocationMatch "/payment/(ppc2|nisc).cgi">
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </LocationMatch>

    <Location /payment/volpay.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/paymentoptions.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlSendHeader ON
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /dcc/dcc_success.cgi> 
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/affiliate/aaaffiliate.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/cartremote.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /fulfillment/index.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /npayment/>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /testvbv/>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    # added for auth cookie logout script
    <Location /newadmin/logout.cgi>
      AuthType PlugNPay::AuthCookieDBI
      AuthName PNPADMINID
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /private/logout.cgi>
      AuthType PlugNPay::AuthCookieDBI
      AuthName PNPPRIVID
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      PerlResponseHandler ModPerl::Registry
      SetHandler perl-script
      Options +ExecCGI
    </Location>

    <Location /admin/logout.cgi>
      AuthType PlugNPay::AuthCookieDBI
      AuthName PNPADMINID
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /billpay/logout.cgi>
      AuthType PlugNPay::BillPay::AuthCookieDBI
      #AuthType PlugNPay::AuthCookieDBI
      AuthName PNPBILLPAYID
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /admin/courtpay/logout.cgi>
      AuthType PlugNPay::AuthCookieDBI
      AuthName PNPADMINID
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /login_billpay.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>

    <Location /payment/emv.cgi>
      PerlSetEnv PERL5LIB /home/p/pay1/perl_lib
      PerlOptions +ParseHeaders
      SetHandler perl-script
      PerlResponseHandler ModPerl::Registry
      Options +ExecCGI
    </Location>
